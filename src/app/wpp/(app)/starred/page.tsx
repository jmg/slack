import { StarredView } from "@/components/wpp/starred-view";

/** `/wpp/starred` — the bookmark list, read live so unstarring updates it. */
export default function WppStarredPage() {
  return <StarredView />;
}
